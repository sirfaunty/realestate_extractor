# Node
node_modules/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
env/
.venv/

# SQLite databases (generated at runtime)
*.db
*.db-journal
*.db-wal
*.db-shm

# DuckDB warehouse (generated at runtime)
*.duckdb
*.duckdb.wal

# Data directories (user uploads, processed files)
data/
!data/mpls_*.json
uploads/
output/
outputs/

# Partner data (large parquets, CSVs)
*.parquet

# Large binary files (keep local, don't push)
*.msg
Ownership Deliverables/*.pdf

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
__MACOSX/

# Environment / secrets
.env
.env.local
*.pem
*.key

# Test artifacts
test_*.db

# Logs
*.log

# Large ZIP uploads (stage locally, don't push)
*.zip

# Barrington source financials (keep local, do not commit)
barrington_db/source_docs/
barrington_db/data/
barrington_db/*.xlsx

# Southtown lease-abstraction warehouse — local-only (source docs + text-laden DBs never leave device)
southtown_db/data/
southtown_db/source_docs/
southtown_db/**/*.db
southtown_db/**/*.docx
southtown_db/_preview*
southtown_db/**/*.xlsx
southtown_db/_rpv*

# Midway disposition-diligence — local-only (source docs + text-laden DBs never leave device)
midway_db/data/
midway_db/source_docs/
midway_db/**/*.db
midway_db/**/*.docx
midway_db/_*

# Data export bundle for mirroring to another machine (local-only)
capactive_data_export.zip
data/warehouse_export/
_import/

# staged source documents (re-extractable from inbox zips)
portfolio_ownership/re_import/staging/
portfolio_ownership/pilot_hadley/sources/

# sync client config (contains device token) + synced PDF store
capactive_sync.ini
data/synced_pdfs/

# CLIENT DATA — never in git (docs/PACKAGING_DESIGN.md; copy between
# machines via export_for_mac.py, not version control)
portfolio_ownership/KA_OWNERSHIP_MODULE_MASTER_20260724/
portfolio_ownership/inbox/
portfolio_ownership/residential/
